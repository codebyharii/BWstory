/**
 * Web stub for @react-native-community/blur
 * Metro picks up the .web.js extension on web builds,
 * so the native module is never resolved.
 */
import React from 'react';
import { View } from 'react-native';

export const BlurView = ({ children, style, ...rest }) => (
  <View style={style}>{children}</View>
);

export const VibrancyView = ({ children, style, ...rest }) => (
  <View style={style}>{children}</View>
);
